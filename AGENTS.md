---
description: >-
  Mapa compacto del repo Bot Cotizador "Alex" (Next.js 14 + DeepSeek).
  Léelo ANTES de explorar: describe arquitectura, módulos, comandos y trampas
  para responder sin gastar tokens escaneando archivos.
version: 1.5.0
alwaysApply: true
---

# AGENTS.md — Contexto del repositorio para Roo Code + DeepSeek

> **LEE ESTE ARCHIVO ANTES DE EXPLORAR EL REPOSITORIO.** Roo Code lo carga
> automáticamente en cada conversación. Es el mapa del proyecto: úsalo para
> responder sin escanear archivo por archivo (ahorra tokens). Solo abre un
> archivo concreto cuando necesites su lógica exacta.

## ⚡ Cómo usar este archivo (reglas de ahorro de tokens)

1. Empieza SIEMPRE aquí. **No** hagas `list_dir`/`find` recursivos "para explorar": lo esencial ya está abajo.
2. Para saber qué tocar, ve a **§11 Tareas → dónde tocar** y abre SOLO ese archivo.
3. Para responder de arquitectura/precios/flujo, usa este archivo (no abras los módulos).
4. Antes de afirmar que algo "existe/compila", checa **§0 Estado de la rama** — en `prospecting-wip` hay piezas rotas o ausentes.
5. Si un archivo citado no aparece en disco, NO lo inventes: confírmalo y avísalo.

---

## 0. Estado actual de la rama (IMPORTANTE)

- **Rama actual:** `prospecting-wip`. En producción (Vercel) corre `main` = cotizador core, sin prospección.
- **✅ Cotizador "Alex" (`app/`, `components/`, `lib/` del cotizador):** completo y validado (regresión **436 asserts** — incluye QA11 de robustez de la cartera, FASE 4 del QA —, eval **30 personas / 568 asserts / 0 fallos / 0 warnings** — 11/11 categorías + 5/5 verticales N4 + 3/3 ecosistemas N5 (marketplace/saas/erp), lint 0, tsc SOLO los 28 errores preexistentes de `prospecting/closing/negotiation-agent.ts`). Es lo que corre en prod.
- **✅ CARTERA COMPLETA (FASES 1-8 del plan `docs/IMPLEMENTACION_CARTERA_ROO.md`):** el motor detecta y cotiza **11 categorías** (las 6 originales + corporativo + menu_digital + tarjeta_digital + link_in_bio + cotizador) y **webapp se amplió a Nivel 4 (verticales: inmobiliaria/membresías/cursos/telemedicina/directorio) y Nivel 5 (marketplace/SaaS/ERP)** por features + señales pasivas. `docs/MERCADO_PAGINAS_VIBECODER.md` documenta la cartera de 28 tipos. Precio único (regla #7) verificado: **motor = UI = PDF = copy = pack** en las 11 categorías (FASE 8).
- **⚠️ Prospección (`prospecting/`):** parcial. En esta rama:
  - **Compila y existe:** `ingest/` (búsqueda determinista + agente LangChain), `outreach/message-chain.ts`, `whatsapp/` (bot, anti-ban, state-manager, queue-service), `scheduler/`, `worker-entry.ts`, `discovery-queue.ts`, `redis.ts`, `store/` (`dashboard-repo.ts`, `leads-repo.ts`), `demo-leads.ts`, `config.ts` (solo `getAntiBanConfig`).
  - **ROTO (no compila):** `prospecting/closing/negotiation-agent.ts` — importa `getClosingConfig`/`getLlmDailyTokenBudget` (ausentes de `config.ts`) y `../store/llm-usage-repo` (no existe). No tocarlo salvo que pidan arreglarlo.
  - **NO existen aún** (no los busques en disco): migraciones `0004`/`0005`, ruta `/api/quote-complete`, `lib/alex-chat-chain.ts`, `lib/intent-llm.ts`, `prospecting/whatsapp/monitor.ts`, `prospecting/closing/{closing-queue,closing-pdf,link-sender,mockup-generator}.ts`.
- **📄 Docs de la cartera:** `docs/IMPLEMENTACION_CARTERA_ROO.md` (plan por fases, fase 8 = QA integral + documentación) y `docs/MERCADO_PAGINAS_VIBECODER.md` (evaluación de mercado de los 28 tipos). Packs generados en `docs/prompts/PACK-*.md` (18 escenarios, regenerables con `npx tsx scripts/generate-pack-samples.ts`).
- **📄 QA de la cartera (cerrado):** `docs/IMPLEMENTACION_QA_CARTERA_ROO.md` (plan por fases, rol por fase). **HECHAS: FASES 0-7 (QA COMPLETO)** — línea base verificada (0), personas 24-30 que cierran cobertura 30/30 en eval (1-3), **QA11 en `scripts/regression-test.ts` (FASE 4, 19 asserts)** blindando: negación de verticales/N5 → landing, degradación por rechazo de webapp, categorías de entrada (menu_digital/tarjeta_digital/link_in_bio/cotizador) sin preguntas pesadas (auth/db/pagos/PWA/referencia), N5 sin precio cerrado (recap "propuesta formal" + pack con regla #7), bots de entrada, escalera de cross-sell y precios motor=fallback, y **FASES 5-6 (pack técnico)** — auditoría (5) e implementación (6) en `lib/prompt-builder.ts`: RF de cartera en `buildFunctionalRequirements` (RF-18+ solo si la señal está activa: multilingüe, ecommerce-pro, verticales N4, ecosistema N5 + RF "propuesta formal"), `WEBAPP_SPECS` + `resolveWebappSpec` (spec de páginas/tablas/flujos/integraciones por vertical N4 y ecosistema N5), línea `NIVEL 5` en el contexto compacto y briefs de marketplace/saas con "propuesta formal detallada" (validado: regresión 436 OK, eval 30/568/0/0, lint 0, tsc sin errores nuevos, 18 packs regenerados sin "CHAT undefined"). **FASE 7 (hecha): validación integral + documentación** — regresión 436 OK, eval 30/568/0/0 (cobertura total 11/11 + 5/5 + 3/3), lint 0, tsc solo los 28 preexistentes de negotiation-agent.ts, prompt:preview y quote:preview OK; coherencia de precios regla #7 verificada **motor = fallback = pack en 19 configuraciones** (11 categorías + 5 verticales N4 + 3 ecosistemas N5, script temporal en scripts-tmp/ borrado); eval:llm opcional corrido en las personas nuevas 24-30 (26/27/30 limpias; 25/28/29 mostraron deriva transitoria de trato en `technical_bots` — no determinista, ya prohibida por el system prompt) y **fix de causa raíz en `lib/personality.ts` `toUsted`**: las reglas `por ti`/`para ti` tenían un byte backspace literal (0x08) en vez del límite de palabra `\b` (no convertían nunca) + añadido `contaste`→`contó` (ver trampa 22 en §10).

---

## 1. Qué es este proyecto

**Bot Cotizador "Alex"** — Aplicación full-stack (Next.js 14 App Router + TypeScript estricto + Tailwind + shadcn/ui) que:

1. **Entrevista de forma conversacional** (NO formulario) a clientes potenciales de webs/landings para generar una **cotización**.
2. **Analiza las respuestas con DeepSeek** y genera una **propuesta personalizada** (precio MXN, alcance, stack, giro, asistentes IA) + un **prompt técnico descargable para Roo Code** (pack de 20–26 chats por fases según bots: 20 sin asistentes IA, 26 con ellos — empieza con estrategia UX e investigación, arquitectura de información + wireframes, brand y contenido real (kickoff), conversation design/microcopy; incluye analítica dividida en instrumentación + reporting, infraestructura LLM/MLOps, asistentes IA, prompt engineering + evaluación, knowledge base + RAG por separado, QA de IA, pruebas unitarias e integración, CI/CD, QA web, seguridad, rendimiento, cumplimiento, SRE, despliegue y presentación/aprobación/crecimiento post-lanzamiento; se afina por tipo de servicio: rol líder, conversión #1, criterios de éxito y riesgos del giro, y cada chat asume un ROL. Cada fase lleva su PRIORIDAD de entrega: **⭐ OBLIGATORIA** (imprescindible para que la web quede profesional y lista para entregar al cliente: UX, wireframes, kickoff, fundación, shell, contenido, microcopy, datos, lógica, QA web, seguridad, rendimiento, cumplimiento y despliegue) o **✨ OPCIONAL** (no bloquea la entrega: analítica, pruebas, CI/CD, SRE, post-lanzamiento y las 6 fases de asistentes IA si el cliente las contrató).
3. Genera **PDF de propuesta comercial** (jsPDF) y **PDF de cotización**.
4. Tiene un **sistema de prospección parcial** (rama `prospecting-wip`): busca leads, les escribe por WhatsApp (Baileys) y los gestiona en un dashboard; el cierre/entrega (PDF/mockup/negociación) aún está incompleto o roto (ver §0).

**Categorías que cotiza el motor (11):** landing · corporativo · ecommerce (con nivel 3 "pro" por features) · citas/reservas (con pago por adelantado) · webapp (con Nivel 4 por vertical: inmobiliaria/membresías/cursos/telemedicina/directorio, y Nivel 5: marketplace/SaaS/ERP — se cotizan con "desde" + propuesta formal, no precio cerrado) · blog · portafolio · **menu_digital** · **tarjeta_digital** · **link_in_bio** · **cotizador**. Los 28 tipos de la agencia están en `lib/agency-catalog.ts` y documentados en `docs/MERCADO_PAGINAS_VIBECODER.md`.

- **En producción:** https://botcotizador.vercel.app (Vercel, GitHub `main` auto-deploy).
- **LLM:** DeepSeek (nativo `DEEPSEEK_API_KEY` o vía OpenRouter `OPENROUTER_API_KEY`). Sin keys → fallback determinista local (la app funciona igual).
- **Stack:** Next 14 · TS strict · Tailwind 3 · shadcn/ui · Zustand · Framer Motion · Supabase · LangChain · Baileys (WhatsApp) · BullMQ (Redis) · jsPDF · Zod.

---

## 2. Comandos útiles

| Comando                                      | Qué hace                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run dev`                                | Dev server (localhost:3000).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `npm run build`                              | Build de producción. ⚠️ NO correr mientras `dev` está activo (corrompe `.next` → `rm -rf .next` y reiniciar).                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `npm run lint`                               | ESLint (Next).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `npm run test:regression`                    | Regresión del motor del bot (436 asserts, incluye QA11 de robustez de la cartera, tsx).                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `npm run test:prospecting`                   | Agente de descubrimiento de leads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `npm run test:message`                       | Generador de mensajes WhatsApp.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `npm run test:bot`                           | Bot WhatsApp (Baileys, 32 asserts).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `npm run test:eval`                          | Personas + deriva del LLM — DETERMINISTA (0 tokens, CI-safe): **30 personas (11/11 categorías**: landing, ecommerce, citas, webapp, blog, portafolio, corporativo, menu_digital, tarjeta_digital, link_in_bio, cotizador **+ 5/5 verticales N4**: inmobiliaria, membresías, cursos, telemedicina, directorio **+ 3/3 ecosistemas N5**: marketplace, saas, erp) por la máquina de estados (flujo cierra, sin redundancia, discovery corto, datos limpios, fallback sano). Imprime cobertura de categorías, verticales y ecosistemas al final. |
| `npm run test:eval:llm`                      | `test:eval` + pinta cada turno con DeepSeek real (`lib/chat-llm.generateNextMessage`) y evalúa deriva: heurísticas (trato mezclado, promesa de monto → FALLO; tema no tocado/contacto fuera de turno/precio fuera de turno → warning) + LLM-as-judge (score 1-5, on_goal=false → FALLO). Necesita key en .env.local. ~$0.05/corrida. Filtros: `--persona N`, `--no-judge`.                                                                                                                                                                   |
| `npm run prompt:preview`                     | Previsualiza el pack de prompts técnicos para Roo (por fases).                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `npm run proposal:preview` / `quote:preview` | Previsualiza PDF propuesta / cotización.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

---

## 3. Mapa de carpetas (visión general)

```
app/
  page.tsx, layout.tsx          → Landing + root layout
  chat/page.tsx                 → El chatbot "Alex" (flujo conversacional)
  results/page.tsx              → Pantalla de resultados/propuesta (lee sessionStorage)
  demo/page.tsx                 → Demo pública de prospección en vivo (SSE)
  login/page.tsx                → Login del dashboard de prospección
  (prospecting)/dashboard|campaigns|conversations|leads  → Dashboard (route group auth)
  api/                          → Rutas serverless (ver §6)
components/
  chat/                         → UI del chat (ChatContainer, ChatInput, ConversationEngine, etc.)
  results/                      → UI de la propuesta (ProposalView, PriceCard, ValueSelling, etc.)
  dashboard/                    → UI del dashboard de prospección (LeadTable, CampaignBuilder, ChatView, StatsCards...)
  ui/                           → primitivas shadcn/ui
lib/                            → LÓGICA NUCLEO (ver §5) — el 90% de la lógica vive aquí
prospecting/                    → Sistema de prospección WhatsApp (ver §7)
scripts/                        → Tests y previews (tsx, deben ser type-safe): regression-test.ts, generate-pack-samples.ts (regenera los packs en docs/prompts/)
supabase/
  schema.sql                    → Esquema del cotizador (tabla leads)
  migrations/0001..0003*.sql    → Migraciones del sistema de prospección
docs/                           → Documentación (MERCADO_VIBECODER.md, MERCADO_PAGINAS_VIBECODER.md, IMPLEMENTACION_CARTERA_ROO.md, prompts/PACK-*.md)
types/index.ts                  → Tipos compartidos
```

- Alias de imports: `@/*` → raíz del proyecto (tsconfig paths).
- Tipos de dominio: `lib/types.ts` + `types/index.ts` + `components/dashboard/types.ts`.

---

## 4. Arquitectura del flujo del bot (importante)

```
cliente → Chat (Zustand store: lib/chat-store.ts)
   │
   ├─ 1) Máquina de estados DETERMINISTA decide el nodo (lib/conversation-flow.ts, árbol)
   │     - Cada turno: node.nextNode(userText, ctx) → siguiente nodo o clarificación
   │     - Nodos con `condition` se SALTAN si el contexto ya lo resolvió (ej. pagos=false)
   │     - El ESTADO nunca se pierde: la máquina manda, el LLM solo pinta texto
   │
   ├─ 2) LLM redacta el mensaje del turno (lib/chat-llm.ts → /api/chat → DeepSeek)
   │     - TURN_GOALS por nodo, contexto compacto, tono "consultor senior"
   │     - Fallback determinista si no hay key / timeout 9s / circuit breaker
   │
   └─ 3) Al terminar → /api/analyze (DeepSeek) → enrichCommercial() (ajusta precio/copy)
         → resultado en sessionStorage (clave "bot_cotizador:result") → /results
         → /results renderiza propuesta + PDFs + prompt.txt descargable
```

**Regla de oro:** el LLM **nunca** decide el flujo; solo genera texto. La máquina de estados (`conversation-flow.ts` + `chat-store.ts`) garantiza robustez.

**Flujo consolidado (menos preguntas):** `pages → technical_bundle → design → technical_bots → scope_content → scope_services → budget → contact_name → extra_comments`.

- **`technical_bundle` (nodo consolidado):** reemplaza la cadena de preguntas sí/no técnicas por UNA pregunta multi-opción con las funciones relevantes aún desconocidas (panel, mapa, WhatsApp, citas, SEO, app instalable, cuentas, BD, pagos, documentos — solo las relevantes a la categoría). Mensaje: "dime cuáles te interesan; lo que no elijas lo dejamos fuera". Quick replies: "Las que me convengan" (aplica recomendaciones por categoría) / "Todas" / "Ninguna". Las preguntas individuales (`technical_auth`/`technical_db`/`technical_payments`/`technical_dashboard`/`technical_maps`/`technical_pdfs`/`technical_chat`/`technical_bookings`/`technical_seo`/`technical_pwa`) quedan como FALLBACK: solo se preguntan si su flag sigue `null`.
- **Inferencia ampliada:** `SIGNAL_PATTERNS`/`NEGATIVE_SIGNAL_PATTERNS` cubren también mapas/chat/SEO/PWA/documentos/animaciones y se ejecutan en `discovery_business` Y `pages`: si el cliente menciona "WhatsApp/Google/mapa/ubicación/cómo llegar/moderno..." en su descripción o en las secciones, esas preguntas se SALTAN (nodos con `condition: ctx.X === null`). `design` se salta si el estilo ya se infirió (`ctx.animaciones !== null`).
- `budget` pregunta fecha Y monto en un solo turno (si el cliente da solo uno, `clarify_budget` afina el que falta, máx 2); `contact_name` pide nombre + correo + WhatsApp en un solo mensaje (`contact_email`/`contact_phone` quedan como seguimientos si falta algo). Cierre: `budget → contact_name → (contact_email → contact_phone si falta) → extra_comments`.

**Analítica de sesión (UX):** el chat emite eventos reales (session_start, node, user_message, no_se, skip, conversion, abandono por pagehide) vía `lib/chat-analytics.ts` (sendBeacon, sobrevive al reload de /results con sessionStorage) → `POST /api/analytics` (tablas `chat_sessions`+`chat_events` si hay Supabase; buffer en memoria + `GET /api/analytics` para debug si no). `/results` marca la sesión como completada con categoría/nivel/precio. Sirve para medir dónde abandonan y recortar el discovery con datos, no con intuición.

---

## 5. Módulos clave en `lib/` (uno-línea)

| Módulo                                                  | Rol                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `conversation-flow.ts`                                  | Árbol de nodos del chat + `extractSignals`/`extractSections`/`extractBudgetAmount`/`extractDeadline`/`captureEarlyData` (parseo de intención, presupuesto, plazo, estructura web). Flujo consolidado: `technical_bundle` (multi-opción) + `budget` fusiona fecha+monto, `contact_name` pide los 3 datos de contacto, y cuentas/BD se saltan para landing/portafolio/blog + las categorías de entrada (menu_digital/tarjeta_digital/link_in_bio/cotizador, `CATEGORIAS_SIMPLES` — FASE 4/QA11: el cotizador es simple, los PDFs/documentos se tratan aparte). + CxD: `quickRepliesFor`/`PHASES`+`getPhase`/`inputHintFor`/`buildRecap`/`sugerirEscaleraProducto`+`GIRO_ESCALERA` (cross-sell en `technical_bots`). Señales PASIVAS de nivel 3/4/5 (no van en el bundle): ecommerce pro (`inventario`/`reportesVentas`/`facturacionCfdi`/`multiVendedor`), plataformas por vertical (`inmobiliaria`/`membresias`/`cursos`/`telemedicina`/`directorio`) y ecosistema N5 (`marketplace`/`saas`/`erp`, con retorno temprano decisivo) → activan features de webapp. `buildRecap` nombra el tipo N2/N4/N5 (y para N5 dice "propuesta formal, no precio cerrado").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `chat-store.ts`                                         | Store Zustand + `sendUserMessage` (motor del turno), skip condicional, retry /api/analyze, persistencia sessionStorage.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `personality.ts`                                        | Personalidad "consultor senior" + `classifyIntent`/`hasSignal`/`extractName`/`extractEmail`/`normalizePhone`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `chat-llm.ts`                                           | LLM de redacción de turnos (DeepSeek vía `lib/llm-client.ts`), circuit breaker, fallback determinista.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `llm-client.ts`                                         | Cliente LLM unificado: OpenRouter o DeepSeek directo (`chatCompletion`, retry backoff).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `openrouter.ts`                                         | Análisis del cliente (`/api/analyze`): `enrichCommercial()` (regla #7: `precio_min = totalExacto` SIEMPRE — el motor manda sobre el clamp por giro; copy adaptado, `filtrarPorDeclinados`, `normalizarArraysResultado`). `localFallback`: mapea señales pasivas → features (ecommerce pro SOLO si category==="ecommerce"; features verticales N4 y ecosistema N5 SOLO si category==="webapp" y la señal está activa — no inflan otras categorías). `buildAiPrompt` genera el pack con el total del motor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `pricing-catalog.ts`                                    | Catálogo de tipos de web (11 categorías) + `inferCategory` (keyword-aware) + `buildFallbackProposal` (fallback determinista; regla #7: `precio_min = totalExacto` SIEMPRE). Categorías de entrada: menu_digital (base 3500/6000/9000), tarjeta_digital (3500/6000/9000), link_in_bio (2500/4500/7000), cotizador (15000/25000/40000). Corporativo = categoría propia (15000/25000/40000, solo con señales claras de multi-página/empresa, NO palabras sueltas "empresa"/"nosotros"). Nivel 4: webapp tiene 17 features por vertical (inmobiliaria: filtros/leads/panel; membresías: cobro recurrente/área privada/planes/retención; cursos: lecciones/progreso/certificado/foros; telemedicina: expediente/videollamada/recetas; directorio: fichas/búsqueda-mapa/ficha premium). Nivel 5: 10 features N5 (split_pagos/multi_tenant/planes_billing/api_publica/módulos ERP/integracion_contable/reportes_ejecutivos). `inferCategory` mapea portales por vertical → webapp (retorno temprano decisivo `VERTICAL_PLATFORM_RE`/`NIVEL5_ECOSYSTEM_RE` con guarda "página sencilla" + bonus; NO roba landings/citas con palabras sueltas tipo "gimnasio"/"curso").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `industry-pricing.ts`                                   | GIROS (abogado/médico… = alto; tienda/mecánico = ajustado), `detectarGiro`, `ajustarPrecio`, `adaptarCopyGiro`, `generarValorNegocio`, `filtrarPorDeclinados`. Giros de nivel 4 validados para no romper el clamp de plataformas: inmobiliaria [25000,70000], gym [10000,28000], consultor/coach [15000,45000], médico/clínica [20000,60000], webapp default [20000,60000]. Giro NUEVO `empresa_operacion` "Empresa / operación (plataforma a medida)" tier ALTO [40000,150000] (keywords SEGURAS: marketplace, comision por venta, control de almacen… NUNCA "erp"/"crm"/"nomina"/"logistica" sueltos porque `includes()` no tiene límites de palabra). OBSERVACIÓN preexistente: un "directorio" sin giro claro cae en "Tienda / comercio local" (keyword "comercio") y el clamp lo baja — refinar si se quiere. OJO: en `detectarGiro` la keyword "consultor" es subcadena de "consultorio" → "consultorio dental" debe caer en médico/dentista (se añadió "consultorio" a sus keywords).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `quote-engine.ts`                                       | Motor de cotización DETERMINISTA (Fase 1): tipos web (landing/corporativo/agenda/menu_digital/tarjeta_digital/link_in_bio/cotizador) + precios, IVA 16%, `calcularTotalDeterminista` (compartido server/UI). `derivarTipoWeb` mapea: citas→agenda, categorías de entrada→su propio TipoWeb, corporativo/ecommerce/webapp→corporativo (base 15000), paginas>3→corporativo. OJO diseño: webapp vertical/N5 muestra $20,300 en la UI (el motor manda; el clamp del giro solo aparece en el rango superior del pack/PDF).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `prompt-builder.ts`                                     | Genera el **pack de 20–26 prompts para Roo Code + DeepSeek** (mobile-first, por fases, acabado premium; 26 si hay asistentes IA, 20 si no; empieza con estrategia UX + wireframes y brand/contenido real (kickoff) y conversation design/microcopy; incluye fases de datos y analítica, knowledge base/RAG con pgvector (solo con bots), pruebas unitarias e integración, CI/CD, QA web + QA de IA, seguridad/OWASP, rendimiento/Performance Engineering, accesibilidad/privacidad/IA responsable, SRE de confiabilidad/observabilidad/operaciones y post-lanzamiento con SEO local/crecimiento; el pack se afina por tipo con `CATEGORY_BRIEFS` + `VERTICAL_BRIEFS` (verticales que NO son categoría: reservas_restaurante/ecommerce_pro/verticales N4/N5) + `resolveBrief(base)` (context-aware, prioridad marketplace>saas>erp>inmobiliaria>…) + fila "Rol que conduce el proyecto" + línea `TIPO DE PÁGINA:` en `buildCompactContext` vía `buildTipoPagina`; cada chat asume un ROL. FASE 6: `buildFunctionalRequirements` añade RF-18+ SOLO si la señal está activa (multilingüe, ecommerce-pro inventario/reportes/CFDI/multi-vendedor, verticales N4 inmobiliaria/membresias/cursos/telemedicina/directorio, ecosistema N5 marketplace/saas/erp + RF "propuesta formal detallada, no precio cerrado"), y `WEBAPP_SPECS` + `resolveWebappSpec` dan spec propio de webapp por vertical/N5 (pages/dataModel/userFlow/integrations → alimentan sitemap, secciones, `dataModelSql` y RF-16); línea `NIVEL 5` en el contexto compacto y briefs de marketplace/saas con "propuesta formal detallada". Regla #7: la fila "Presupuesto estimado" cita el total del motor (min==max → un solo valor); packs reales regenerables con `scripts/generate-pack-samples.ts` → `docs/prompts/PACK-*.md` (18 escenarios). |
| `commercial-proposal.ts` + `commercial-proposal-pdf.ts` | Propuesta comercial formal (Directora de Ventas) + PDF jsPDF.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `generate-proposal.ts` + `proposal-pdf.ts`              | Cotización PDF (6 páginas, formato estricto).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `bots-catalog.ts`                                       | 12 bots IA vendibles (add-on) con precios, `detectarBotsRecomendados`, `extraerBotsDeRespuesta`. FASE 6: NUEVA matriz `GIRO_CROSS_SELL` (giro → bots ordenados, máx 3: restaurante→faq/citas/recomendador, estética→citas/leads/faq, clínica→citas/faq/ventas, gym→membresias/leads/faq, tienda→dudas/ventas/leads, inmobiliaria→ventas/leads/faq, consultor→membresias/ventas/leads, mecánico→faq/leads/cotizacion); su ORDEN manda y las reglas genéricas se anexan al final (bot_faq genérico NO desplaza a ventas/recomendador/membresias). Guardas: `Object.hasOwn` para giros tipo "constructor" (Object.prototype); cotizador NO recomienda bot_cotizacion. Nivel 4: por señal vertical → inmobiliaria=bot_ventas+bot_leads, membresías=bot_membresias+bot_leads, cursos=bot_membresias+bot_ventas, telemedicina=bot_citas+bot_faq, directorio=bot_faq+bot_leads (tope 3 intacto).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `agency-catalog.ts`                                     | ~28 tipos de web para la agencia. FASE 1: menu_digital (3500), tarjeta_digital (4500), link_in_bio (2500), cotizador (15000) con `categoriaBase` a sus categorías. Nivel 4: inmobiliaria 25000, membresías 28000, curso_online 30000, directorio 22000, telemedicina 26000 — todos `categoriaBase: "webapp"` (telemedicina cambió de "citas" a "webapp" en FASE 4). Nivel 5: marketplace 40000, marketplace_split 70000, saas 60000, erp 90000 — `categoriaBase: "webapp"`. Documentados en `docs/MERCADO_PAGINAS_VIBECODER.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `supabase.ts` / `prospecting-auth.ts`                   | Cliente Supabase (server) / auth del dashboard (cookie HMAC).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

---

## 6. Rutas API (`app/api/`)

- `chat` → redacta el mensaje del turno con DeepSeek (`llm:true`).
- `analyze` → analiza la conversación completa → propuesta JSON (`fallback:true/false`).
- `save` → persiste lead/propuesta en Supabase (tabla `leads`).
- `login` / `logout` → auth dashboard (ADMIN_PASSWORD + AUTH_SECRET).
- `search` → encola discovery de leads (BullMQ, 503 si no hay REDIS_URL).
- `campaign` → crea campaña + mensajes WhatsApp (memoizados por categoría).
- `stats` / `leads` / `leads/[id]` / `webhook` → dashboard + webhook WhatsApp.
- `demo` → SSE de la demo en vivo (EventSource), búsqueda + mensajes simulados.

---

## 7. Sistema de prospección (`prospecting/`)

Pipeline de ventas **parcial** (rama `prospecting-wip`). Solo las etapas 1-3 están operativas; las 4-6 incompletas o rotas (ver §0).

```
1. Prospección  ingest/search-agent.ts (agente LangChain + SerpAPI + hasWebsite) o ruta determinista (search-local, 0 tokens)
2. Ventas       outreach/message-chain.ts (mensajes WhatsApp personalizados, memoizados por categoría)
3. Diagnóstico  = el bot Alex (/chat) — el lead cotiza
4-6.           Cierre/entrega/resumen → PENDIENTE o ROTO (solo negotiation-agent.ts, sin compilar)
```

Módulos (los que existen en disco):

- `worker-entry.ts` → bootstrap de 2 colas: `whatsapp-outbound` + `discovery` (BullMQ si hay `REDIS_URL`; si no, memoria/noop).
- `whatsapp/` → `whatsapp-bot.ts` (Baileys v6, QR), `state-manager.ts` (estados `pending→sent→responded→interested→meeting→client` + `no_response`/`blacklist`), `anti-ban.ts` (daily_limit, horario, delay aleatorio), `queue-service.ts` (cola outbound). ⚠️ NO existe `monitor.ts`.
- `ingest/` → `search-agent.ts` (agente LangChain), `search-local.ts` (SerpAPI + `has-website.ts` determinista), `search-cache.ts` (cache 24h).
- `scheduler/` → node-cron: drena pendientes en horario hábil, marca stale (`no_response`).
- `closing/` → SOLO `negotiation-agent.ts` (híbrido máquina+LLM, escalera de tácticas) — **roto, no compila** (imports ausentes, ver §0).
- `store/` → `dashboard-repo.ts` (leads/campañas/mensajes con fallback en memoria) + `leads-repo.ts` (batch insert, upsert).
- `config.ts` → `getAntiBanConfig()` (horario/delay; solo lee `PROSPECT_TZ` del env).
- `discovery-queue.ts` / `redis.ts` / `demo-leads.ts` → cola discovery, conexión Redis, leads ficticios para la demo.

---

## 8. Datos / Supabase

- **Cotizador:** tabla `leads` (schema.sql).
- **Prospección:** migraciones existentes `supabase/migrations/0001..0003*.sql`:
  - `0001` → `prospect_leads` + `search_cache` (RLS cerrada).
  - `0002` → estados WhatsApp (`sent/responded/interested/…/client` + `no_response`/`blacklist`) + `wa_send_log`.
  - `0003` → `campaigns` + `campaign_leads` + `conversation_messages`.
  - ⚠️ `0004`/`0005` (client_quotes/negotiation_events, estados de cierre) NO existen en este checkout.
- Sin Supabase/Redis configurados → todo cae a fallbacks en memoria/deterministas (la app funciona en dev sin infra).

---

## 9. Variables de entorno (`.env.local`)

- **LLM:** `DEEPSEEK_API_KEY` / `DEEPSEEK_MODEL` (deepseek-chat) · `OPENROUTER_API_KEY` / `OPENROUTER_MODEL` · `NEXT_PUBLIC_LLM_CHAT=1` (0 = solo determinista). Override opcional `DEEPSEEK_BASE_URL`.
- **Supabase:** `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY`.
- **Marca/contacto (placeholders en prod):** `NEXT_PUBLIC_BOT_NAME`, `NEXT_PUBLIC_AGENCY_NAME`, `NEXT_PUBLIC_DEVELOPER_NAME`, `NEXT_PUBLIC_DEVELOPER_WHATSAPP`, `NEXT_PUBLIC_DEVELOPER_EMAIL`.
- **Prospección (verificadas en código):** `REDIS_URL` (vacío → colas en memoria), `SERPAPI_API_KEY` / `SERPAPI_ENDPOINT`, `ADMIN_PASSWORD`, `AUTH_SECRET`, `WEBHOOK_SECRET`, `PROSPECT_TZ`, `BAILEYS_SESSION_DIR`, `WA_SENDER_NAME`, `DEBUG_PROSPECTING`.
- ⚠️ No usar vars de módulos que NO existen en este checkout: `OWNER_WHATSAPP`, `MOCKUP_PROVIDER`, `NEXT_PUBLIC_BOT_URL`, `PROSPECT_DAILY_LIMIT`, etc.

---

## 10. Patrones y trampas críticas (lecciones aprendidas)

1. **NO correr `npm run build` con `npm run dev` activo** en el mismo workspace → corrompe `.next` (errores "Cannot find module ./682.js"). Fijar: `rm -rf .next` y reiniciar dev.
2. **Nunca leer `sessionStorage`/`localStorage` durante el render SSR** (errores React #418/#423). Hidratar SOLO en `useEffect` + estado `ready` con spinner.
3. **Todo nodo con `condition`** en conversation-flow debe devolver su `next` ante respuesta vacía (`if (!response?.trim()) return next`), porque el skip del engine llama `nextNode("", ctx)` y `classifyIntent("")` → "no sé" → caería en clarificación.
4. **`classifyIntent`:** las palabras ambiguas ("eso/ya/bueno/claro/justo/dale/ok") NO cuentan como "sí" si hay negación fuerte (no/nunca/nada/ni/tampoco). "Sí, pero no quiero X" sigue siendo ambigua.
5. **`extractSignals` consciente de negación** (`isNegated`): "no necesito reservar mesas" NO activa citas. **Siempre usar flag `"g"` en `new RegExp`** con `exec()` o hay loop infinito.
6. **`inferCategory` / keywords con palabra completa** (regex con límites), no subcadena ("WhatsApp" contiene "app" → webapp falso).
7. **Precio coherente (regla #7):** UI, PDF, copy y prompt citan el MISMO total. El motor manda: `precio_min = totalExacto` SIEMPRE (no solo con bots) en `buildFallbackProposal` y `enrichCommercial`; el clamp del giro solo queda como extremo superior del rango (`precio_max`) y copy. Usar `calcularTotalDeterminista()` + `resolverCategoria()` + `cuota_mensual = quoteTotal/24`. `filtrarPorDeclinados` quita de la propuesta lo que el cliente rechazó. OJO diseño: webapp vertical/N5 muestra $20,300 en la UI (motor mapea webapp→corporativo base 15000) aunque el estimado por features sea mayor — discrepancia PREEXISTENTE aceptada (el pack/PDF muestran el rango superior honesto).
8. **`Object.hasOwn(OBJ, key)` antes de `OBJ[key]`** en objetos planos indexados por giro/estado cuando la key puede ser `"constructor"`/`"toString"` (giro id "constructor" de constructora devolvía `Object.prototype.constructor` → "not iterable").
9. **tsx/esbuild**: `X ?? a || b` (mezclar `??` con `||` sin paréntesis) lanza error de TRANSFORM (parse, get_errors NO lo ve) → usar `?? (a || b)`.
10. **Keywords de giro con `includes()`** no tienen límites de palabra: "consultor" es subcadena de "consultorio" → "consultorio dental" cae en consultor; añadir "consultorio" a médico/dentista. En N5, NUNCA keywords sueltas "erp"/"crm"/"nomina"/"logistica" (roban subcadenas).
11. **El pack "Presupuesto estimado"** cita `precio_min – precio_max`; con regla #7 (min==max cuando el motor manda) el render cita UN solo valor (sin "X – X"). Al regenerar packs, verificar que NO queden literales "CHAT undefined" (referencias `phases.llmInfra`/bots solo existen con hasBots).
12. **import() dinámico de libs opcionales con VARIABLE**, nunca literal: `const lib="@sparticuz/chromium"; await import(lib)` — si la lib no está instalada y es literal, `next build` falla.
13. **DeepSeek vía SDK OpenAI:** el baseURL no debe incluir `/chat/completions` (el SDK lo añade); `AgentExecutor`/chains usan SSE (mockear con `data: {...}\n\n` + `data: [DONE]`); escapar llaves `{{ }}` en ChatPromptTemplate; no usar `.default()` en zod de un `tool()`.
14. **Modelos/chain se construyen bajo demanda** (memoizados) porque `process.env.*` se lee en tiempo de llamada (los const de módulo se resuelven al importar).
15. **`next build` exige type-safe total** porque `scripts/` entra en el tsconfig → los tests DEBEN compilar (onReceive opcional, `Array.from(Map.entries)`).
16. **jsPDF:** `doc.setTextColor` NO acepta tuplas → usar spread `...PRIMARIO`.
17. **ESLint de Next aplica react-hooks a TODO archivo** → renombrar imports `use*` de libs no-React (alias) en vez de deshabilitar reglas.
18. **Cookie `Secure` condicional a HTTPS**; `isAuthenticated(req)` → `decodeURIComponent` del token (el header raw no decodifica).
19. **En Vercel serverless**, rutas de bot/LLM: `export const runtime = "nodejs"` + `export const maxDuration = 60` (DeepSeek excede el timeout de 10s).
20. **Estado final del closer es `"client"` (inglés)** — el typo "cliente" rompe `canTransition`.
21. **Preguntas consolidadas:** `budget` pregunta fecha+monto juntos; si el cliente da SOLO uno, `nextNode` manda a `clarify_budget` (afina el que falta, máx 2 vía `noSeContador`); el check "ambos ya seteados → `contact_name`" va PRIMERO (aunque la frase diga "no sé cuánto cobran", es retórico si ya dio los dos). `contact_name` extrae los 3 datos con `extractName`+`extractEmail`+`normalizePhone` y deriva a `contact_email`/`contact_phone` solo si falta algo.
22. **Cuidado con bytes corruptos en regex de `toUsted` (`lib/personality.ts`):** las reglas `por ti`/`para ti` → "por usted" tenían un byte **backspace literal (0x08)** en lugar del límite de palabra `\b` — el regex era `/\x08por ti\b/gi` (válido, compila y no rompe tsc) pero NUNCA matcheaba → en conversaciones de "usted" el bot dejaba "por ti"/"para ti" sin convertir y el eval:llm reportaba trato mezclado. Detectar: `grep -n ... | cat -A` muestra `^H` (backspace) donde debería ir `\b` (backslash-b). Verificar con `od -c`/`cat -A` cuando una regla regex "no actúa" pese a existir.

---

## 11. Tareas comunes → dónde tocar

| Tarea                               | Archivo(s)                                                                                                                                                                                                                                   |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cambiar preguntas/flujo del bot     | `lib/conversation-flow.ts` (+ `lib/chat-llm.ts` TURN_GOALS si toca texto LLM)                                                                                                                                                                |
| Cambiar personalidad/tono           | `lib/personality.ts`                                                                                                                                                                                                                         |
| Cambiar precios/tipos de web        | `lib/pricing-catalog.ts`, `lib/quote-engine.ts`, `lib/industry-pricing.ts` (GIROS), `lib/agency-catalog.ts` (precioDesde)                                                                                                                    |
| Añadir categoría/vertical nueva     | `lib/pricing-catalog.ts` (categoría+features+keywords) + `lib/conversation-flow.ts` (señales) + `lib/agency-catalog.ts` + `lib/bots-catalog.ts` + `lib/industry-pricing.ts` (giro) — ver `docs/IMPLEMENTACION_CARTERA_ROO.md` (DoD por tipo) |
| Cambiar el pack de prompts para Roo | `lib/prompt-builder.ts`                                                                                                                                                                                                                      |
| Cambiar la propuesta/PDF            | `lib/commercial-proposal.ts`, `lib/commercial-proposal-pdf.ts`, `lib/generate-proposal.ts`                                                                                                                                                   |
| Cambiar UI del chat                 | `components/chat/`                                                                                                                                                                                                                           |
| Cambiar UI de resultados            | `components/results/`                                                                                                                                                                                                                        |
| Prospección/mensajes WhatsApp       | `prospecting/` (ver §7)                                                                                                                                                                                                                      |
| Dashboard                           | `app/(prospecting)/` + `components/dashboard/`                                                                                                                                                                                               |
| Schema/BD                           | `supabase/schema.sql`, `supabase/migrations/`                                                                                                                                                                                                |
